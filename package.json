{
  "name": "rbk-portfolio-root",
  "private": true,
  "scripts": {
    "dev": "cd app && npm run dev",
    "build": "cd app && npm install && npm run build",
    "install": "cd app && npm install"
  }
}
